export function HomePage() {
  return (
    <main className="app-shell">
      <section className="surface" aria-labelledby="welcome-title">
        <p className="eyebrow">Alira</p>
        <h1 id="welcome-title">Your financial clarity starts here.</h1>
        <p className="lede">
          The frontend foundation is ready. Product experiences will be added in
          focused, tested increments.
        </p>
      </section>
    </main>
  );
}
